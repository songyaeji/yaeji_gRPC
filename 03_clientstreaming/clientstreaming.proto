syntax = "proto3";

option go_package = ".;main";

service ClientStreaming {
    rpc GetServerResponse(stream Message) returns (Number) {}
}

message Message {
  string message = 1;
}

message Number {
  int32 value = 1;
}